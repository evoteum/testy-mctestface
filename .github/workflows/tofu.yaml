name: Call Multi-Tofu Workflow

on:
  push:
  workflow_dispatch:


permissions:
  id-token: write
  contents: write
  actions: write

jobs:
  call-multi-tofu:
    uses: evoteum/estate-reusable-workflows/.github/workflows/multi-tofu.yml@main
    secrets: inherit
